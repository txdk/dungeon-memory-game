// Size of control buttons when viewed on a small device (e.g. mobile)
export const MOBILE_ICON_SIZE: number = 27;

// Size of icons on control buttons
export const ICON_SIZE: number = 42;

// Size of icons in input display on a small device (e.g. mobile)
export const MOBILE_INPUT_ICON_SIZE: number = 20;

// Size of icons in input display
export const INPUT_ICON_SIZE: number = 27;

// Stage transition time in ms
export const STAGE_TRANSITION_DELAY = 2000;

// Colours for app elements
export enum APP_COLOURS {
    PRIMARY = "text-green-500",
    ALERT = "text-red-700",
    DISABLED = "text-gray-500",
    IMPORTANT = "text-yellow-400"
};

// Duration of alert flash in ms
export const ALERT_FLASH_DURATION = 500;